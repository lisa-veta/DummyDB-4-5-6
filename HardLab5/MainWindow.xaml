<Window x:Class="HardLab5.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HardLab5" d:DataContext="{d:DesignInstance Type=local:MainViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20*"/>
            <RowDefinition Height="20*"/>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="480*"/>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="40*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10*" />
            <ColumnDefinition Width="285*" />
            <ColumnDefinition Width="10*" />
            <ColumnDefinition Width="150*" />
            <ColumnDefinition Width="150*" />
            <ColumnDefinition Width="150*" />
            <ColumnDefinition Width="150*" />
            <ColumnDefinition Width="10*" />
        </Grid.ColumnDefinitions>
        <Menu VerticalAlignment="Center" Grid.Column="1" Grid.ColumnSpan="6">
            <MenuItem Header="Файл" >
                <MenuItem Command="{Binding  OpenDataFile}" Header="Открыть файл"/>
            </MenuItem>
        </Menu>

        <TextBlock Grid.Column="1" Grid.Row ="1" Text="Структура исходной базы данных" Background="DarkSeaGreen" ></TextBlock>
        <TextBlock Grid.Column="3" Grid.Row ="1" Text="Таблица" Background="DarkSeaGreen" Grid.ColumnSpan="4"></TextBlock>
        <TextBox Name="TextBox1" Grid.Column="1" Grid.Row ="5"  Foreground="Salmon" >
        </TextBox>
        <TreeView Grid.Column="1" Grid.Row="3">
            <TreeViewItem Name="folderTree" />
        </TreeView>
        <DataGrid Name="DataGrid"  Grid.Column="3" Grid.Row="3"  AutoGenerateColumns="True" ItemsSource="{Binding DataTable}"  CanUserAddRows="False" 
                  RowBackground="LightYellow" AlternatingRowBackground="LightBlue" Grid.ColumnSpan="4"/>
        <Button x:Name="CreateDB" Command="{Binding  CreateNewDB}"   Grid.Column="3" Grid.Row="5" Content="Создать БД" Background="DarkSeaGreen"/>
        <Button x:Name="CreateNewTable" Command="{Binding  CreateNewTable}" Grid.Column="4" Grid.Row="5" Content="Создать таблицу" Background="DarkSeaGreen"/>
        <Button x:Name="Edit" Command="{Binding  EditTable}" Grid.Column="5" Grid.Row="5" Background="DarkSeaGreen">
            <Button.Content>
                <TextBlock Text="Редактировать таблицу" TextWrapping="Wrap" TextAlignment="Center"/>
            </Button.Content>
        </Button>
        <Button x:Name="Update" Command="{Binding  UpdateFile}" Grid.Column="6" Grid.Row="5" Content="Обновить" Background="DarkSeaGreen"/>
    </Grid>
</Window>

    